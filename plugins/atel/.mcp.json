{
  "mcpServers": {
    "atel": {
      "command": "npx",
      "args": ["-y", "@jongleberry/atel", "mcp"],
      "env": {
        "ATEL_URL": "${ATEL_URL}",
        "ATEL_TOKEN": "${ATEL_TOKEN}"
      }
    }
  }
}
