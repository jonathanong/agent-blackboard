{
  "mcpServers": {
    "agent-blackboard": {
      "command": "npx",
      "args": ["-y", "@jongleberry/agent-blackboard", "mcp"],
      "env": {
        "AGENT_BLACKBOARD_URL": "${AGENT_BLACKBOARD_URL}",
        "AGENT_BLACKBOARD_TOKEN": "${AGENT_BLACKBOARD_TOKEN}"
      }
    }
  }
}
