{
  "$schema": "https://unpkg.com/knip/schema.json",
  // Not an npm package — a separately-installed Rust binary (brew/cargo/GitHub release
  // locally, lycheeverse/lychee-action in CI). See CLAUDE.md.
  "ignoreBinaries": ["lychee"],
  // A workspace self-dependency of the root, added only so pnpm links its bin into
  // root's node_modules/.bin (`pnpm exec agent-blackboard` locally). Never imported.
  "ignoreDependencies": ["@jongleberry/agent-blackboard"],
  "workspaces": {
    "packages/agent-blackboard": {
      "entry": ["src/index.mts!", "src/cli/index.mts!"],
      "project": ["src/**/*.mts!", "!src/**/*.test.mts!"],
    },
    "packages/server": {
      "entry": ["src/handler.mts!", "infra/*.mjs"],
      "project": ["src/**/*.mts!", "!src/**/*.test.mts!"],
    },
  },
}
